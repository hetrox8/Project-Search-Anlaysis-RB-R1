<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Results - Trendex</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h1>Search Results</h1>
    <h3>Latest Searches</h3>
    <ul class="list-group mb-4">
      <% @searches.each do |search| %>
        <li class="list-group-item"><%= search.query %> (Searched by IP: <%= search.ip %>)</li>
      <% end %>
    </ul>
    <a href="/trends" class="btn btn-primary">View Trends</a>
    <a href="/dashboard" class="btn btn-primary">View Dashboard</a>
    <form action="/reset" method="POST" style="display:inline;">
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <button type="submit" class="btn btn-danger">Reset Search & Trends</button>
    </form>
  </div>
</body>
</html>
